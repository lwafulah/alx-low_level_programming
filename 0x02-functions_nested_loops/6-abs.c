#include "main.h"
/**
 * _abs - checks the absolute value of an integer
 * @x : paramter to be tested
 * Return: Always x
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
